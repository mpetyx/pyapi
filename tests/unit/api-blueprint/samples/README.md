# API Blueprint Examples
All files in this directory are actual **valid API blueprint**. Since they are saved with the `.md` extension GitHub automatically renders them during viewing.

**To view an API blueprint source make sure to view the file as "raw".**
